<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.edu_sharing</groupId>
    <artifactId>edu_sharing-community-parent</artifactId>
    <version>develop-SNAPSHOT</version>
    <relativePath/>
  </parent>

  <artifactId>edu_sharing-plugin-${rootArtifactId}</artifactId>
  <packaging>pom</packaging>
  <version>${version}</version>

  <name>edu_sharing-plugin-${rootArtifactId}</name>

  <scm>
    <connection>scm:git:https://scm.edu-sharing.com/edu-sharing-plugins/edu-sharing-plugin-${rootArtifactId}.git</connection>
    <developerConnection>scm:git:https://scm.edu-sharing.com/edu-sharing-plugins/edu-sharing-plugin-${rootArtifactId}.git</developerConnection>
    <url>https://scm.edu-sharing.com/edu-sharing-plugins/edu-sharing-plugin-${rootArtifactId}.git</url>
  </scm>

  <properties>

    <community.rendering.version>feature-maven-${project.parent.version}</community.rendering.version>
    <community.repository.version>feature-maven-${project.parent.version}</community.repository.version>

  </properties>

  <dependencyManagement>

    <dependencies>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-rendering</artifactId>
        <version>${community.rendering.version}</version>
        <type>tar.gz</type>
        <classifier>bin</classifier>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-backend-alfresco-fixes</artifactId>
        <version>${community.repository.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-backend-alfresco-module</artifactId>
        <version>${community.repository.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-backend-alfresco-module</artifactId>
        <version>${community.repository.version}</version>
        <type>amp</type>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-backend-services-core</artifactId>
        <version>${community.repository.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-backend-tomcat</artifactId>
        <version>${community.repository.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-backend-webapp</artifactId>
        <version>${community.repository.version}</version>
        <type>war</type>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-config</artifactId>
        <version>${community.repository.version}</version>
        <classifier>sources</classifier>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-community-repository-frontend-webapp</artifactId>
        <version>${community.repository.version}</version>
        <type>war</type>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-deploy-docker-rendering-compose</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-deploy-docker-repository-compose</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-backend-alfresco-fixes</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-backend-alfresco-module</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-backend-alfresco-module</artifactId>
        <version>${project.version}</version>
        <type>amp</type>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-backend-services-core</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-backend-webapp</artifactId>
        <version>${project.version}</version>
        <type>war</type>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-config</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>edu_sharing-plugin-${rootArtifactId}-repository-webapp</artifactId>
        <version>${project.version}</version>
        <type>war</type>
      </dependency>

    </dependencies>

  </dependencyManagement>

  <repositories>

    <repository>
      <id>edu-sharing.public.releases</id>
      <url>https://artifacts.edu-sharing.com/repository/public-releases/</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>

    <repository>
      <id>edu-sharing.public.snapshots</id>
      <url>https://artifacts.edu-sharing.com/repository/public-snapshots/</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <updatePolicy>always</updatePolicy>
      </snapshots>
    </repository>

    <repository>
      <id>edu-sharing.plugin.${rootArtifactId}.releases</id>
      <url>https://artifacts.edu-sharing.com/repository/plugin-${rootArtifactId}-releases/</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>

    <repository>
      <id>edu-sharing.plugin.${rootArtifactId}.snapshots</id>
      <url>https://artifacts.edu-sharing.com/repository/plugin-${rootArtifactId}-snapshots/</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <updatePolicy>always</updatePolicy>
      </snapshots>
    </repository>

  </repositories>

  <distributionManagement>

    <repository>
      <id>edu-sharing.plugin.${rootArtifactId}.releases</id>
      <url>https://artifacts.edu-sharing.com/repository/plugin-${rootArtifactId}-releases/</url>
    </repository>

    <snapshotRepository>
      <id>edu-sharing.plugin.${rootArtifactId}.snapshots</id>
      <url>https://artifacts.edu-sharing.com/repository/plugin-${rootArtifactId}-snapshots/</url>
    </snapshotRepository>

  </distributionManagement>

</project>

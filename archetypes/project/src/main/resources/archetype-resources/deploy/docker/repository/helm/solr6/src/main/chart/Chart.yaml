apiVersion: v2

name: ${helm.prefix}-repository-search-solr6
description: ${helm.prefix}-repository-search-solr6

icon: ${helm.chart.icon}

version: ${helm.chart.version}
appVersion: ${docker.tag}
kubeVersion: "${helm.kube.version}"

# Cleanup
 
- Clean up all data volumes by calling:

  > **IMPORTANT:**     
    Make a [backup](../backup/README.md) before you do this step, because all data will be lost! 

  > If you run multiple instances, see [Common](../../common/README.md#multi-instance-support).
  
  ```
  ./deploy.sh purge 
  ```
